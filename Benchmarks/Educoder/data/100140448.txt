package step3;

public class HelloWorld {

	public void judge(String fileName,String email){
		int sign = fileName.lastIndexOf('.');
      if(fileName.substring(sign+1,fileName.length()).equals("java")&&sign!=0)
          System.out.println("Java文件名正确");
        else System.out.println("Java文件名无效");
       sign = email.indexOf('.');
      if(email.substring(sign+1,email.length()).equals("com")&&sign!=0)
          System.out.println("邮箱名正确");
        else System.out.println("邮箱名无效");
	}
	
	
}
