package task3;
import java.util.Scanner;

public class Box {
    double width;
    double height;
    double depth;
    int boxType;
    public static final int RECTANGLE = 1;
    public static final int CYLINDER = 2;
    
    public double volume(double width,double height,double depth) {
        if(boxType==1) {
            return width*height*depth;
        }else{
            return width*depth*height;
        }
     }
   
       
    
    
    public  static void main(String args[]){
        Box box = new Box();
        Scanner sc = new Scanner(System.in);
       /* box.width = sc.nextDouble();
        box.height = sc.nextDouble();
        box.depth = sc.nextDouble();
        box.boxType = sc.nextInt();*/
        
        
        double v;
            v=box.volume(sc.nextDouble(),sc.nextDouble(),sc.nextDouble());
        System.out.println("the volumn is"+v);
        
    }
}