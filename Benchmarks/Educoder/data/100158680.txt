#include <stdio.h>
int main()
{
	int a[100];
	int i, j, k;
	int f=0;
	int l=0;
	while(scanf("%d",&a[l])!=EOF) l++;
	int t=a[l-1];
	for(i=0;i<l-3;i++)
		for(j=i+1;j<l-2;j++)
			for(k=j+1;k<l-1;k++)
			 if(a[i]+a[j]+a[k]<t){ 

                 f++;
             } 

	printf("%d",f);
		
	return 0;
} 