package step2;

public class IntCalculate {
	// 根据题目要求返回输出结果
	public int IntCalculate(int a, int b){
		// 请在此添加实现代码
        int c;
        c = a + b;
        if (a % b == 0||c >1000)
            return a;
        else
            return b;
    
            
	}
}
