#include<bits/stdc++.h>
using namespace std;
int main()
{
    int a[100];
    int n=0;
    while(1)
    {
        scanf("%d",&a[n]);
        n++;
        char c=getchar();
        if(c=='\n')
        {
            break;
        }
    }
    int T;
    cin>>T;
    int count=0;
    int g=0;
    for(int i=0;i<n;i++)
    {
        for(int j=i+1;j<n;j++)
        {
            for(int k=j+1;k<n;k++)
            {
                if(a[i]+a[j]+a[k]<T)
                {
                    //g=1;
                    count++;
                    //break;
                }
            }
        }
    }
    cout<<count;
}
