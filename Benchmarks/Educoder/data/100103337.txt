package step3;

import java.io.File;
import java.io.FileReader;
import java.io.FileWriter;
import java.io.IOException;

public class DeleteFiles {
    public static void start() throws IOException {
        StringBuilder data = new StringBuilder("Hello World");
        StringBuilder data1 = new StringBuilder("Java is great");
        /********** Begin *********/
        //读取readme.txt文件
		

        //写入readmeCopy.txt文件
		    File file = new File("src/step3/readmeCopy.txt");   ////file对象指向src文件夹下的step1文件夹下的Hello World.txt文件（此时此文件不存在）
    if (!file.exists()) {   //如果文件不存在
        System.out.println(file.createNewFile());   //创建文件，并输出布尔值
    }
        
System.out.println(data);
        System.out.println(data1);
        //删除readme.txt文件
		        
	
        /********** End *********/
    }
}
