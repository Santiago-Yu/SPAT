package step1;
import java.util.*;
public class Equation_Test {
  public static void main(String[] agrs){
     double a,b,c; //方程的三个系数
    /**********begin******/

Scanner scan = new Scanner(System.in);
         double j;
      a=scan.nextDouble();
      b=scan.nextDouble();
      c=scan.nextDouble();
      j=Math.sqrt(b*b-4*a*c);
      if(a==0){
      System.out.printf("无实根\n");
      }else if(j<0){
      System.out.printf("方程无实根信息");
      }else{
          double x1,x2;
          x1=(-b+j)/(2*a);
          x2=(-b-j)/(2*a);
      System.out.printf("X1=%.2f\n",x1);
      System.out.printf("X2=%.2f",x2);
      }


  
  
  
    /**********end*****/
  }
  	
}
