package step3;
import java.util.Scanner;

//根据边长计算长方形的周长及面积，用方法完成。
public class TestJava23_10 {

	public static void main(String[] args) {
		Scanner sc=new Scanner(System.in);
		System.out.println("请输入长方形的长：");
		float c=sc.nextFloat();
		System.out.println("请输入长方形的宽：");
		float k=sc.nextFloat();
		//此处分别调用ZhouChang方法和MianJi方法，求出长方形的周长、面积并输出
		  float x=ZhouChang(k,c);
		  float y=MianJi(k,c);
		    System.out.println("长为："+c+"0"+"，"+"宽为："+k+"0"+"的长方形的周长是："+x+"0"+"，"+"面积是："+y+"0");
	}
	private static float MianJi(float k, float c) {
		// TODO 自动生成的方法存根
		float y=k*c;
		return y;
	}
	/*******Start
	 * @param c 
	 * @param k *********/
	//此处定义一个方法，名为ZhouChang，实现求长方形的周长的功能，并返回

	private static float ZhouChang(float k, float c) {
		// TODO 自动生成的方法存根
		float x=(k+c)*2;
		return x;
	}
	
	
	
	
	/*******End*********/
	//此处定义一个方法，名为MianJi，实现求长方形的面积的功能，并返回
	
	
	
	
	/*******End*********/
}
