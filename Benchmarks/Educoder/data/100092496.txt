package step3;

public class FindMaxNum {
    // 找出三个整数的最大值
    public int FindMaxNum(int a, int b, int c){ //方法五要素：修饰词、返回值类型、方法名、参数列表、方法体
        // 请在此添加实现代码
    
        if(a>b) b=a;
        if(b>c) b=b;else b=c;
        
        
        /* if(a>b) b=a;
        if(b<c) b=c;
        */
        
	return b; //return属于方法体，作用是返回值 
        /* if(a<b) a=b;
        if(a<c) 
            return c;
        else
            return a;
		*/
	}
}
