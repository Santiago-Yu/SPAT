#include <iostream>
#include <stdio.h>
using namespace std;
int main()
{
    int a[60];
    int t;
    int n=0;
    char c;
    int p=0;
    int max=0;
    for(int i=0;i<60;i++)
    {
    	scanf("%d",&a[i]);n++;
        c=getchar();
        if(c=='\n') break;
    }
        scanf("%d",&t);
    
    for(int i=0;i<n;i++){
    	for(int j=i+1;j<n;j++){
        	for(int k=j+1;k<n;k++){
            	if(a[i]+a[j]+a[k]<t) max++;
            }
        }
    }
    
    printf("%d",max);
    
}