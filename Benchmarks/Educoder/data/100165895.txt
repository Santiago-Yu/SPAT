package step4;
import step1.Date;
import step2.Student;
import step3.Course;

public class Report {
    private Student s;//学生基本信息
    private String major; //所学专业  
    private Course[] selCourse;//所选课程
   /************Begin****************/
    public Report(){
		s=new Student();
		for (int i=0;i<10;i++){
        	selCourse[i] = new Course();
    	}
	}
	public Report(Student s, String major, Course[] selCourse){
		this.s=s;
		this.major=major;
		this.selCourse=selCourse;
		// for (int i=0;i<10;i++){
        // 	selCourse[i] = new Course();
    	// }
	}

	public Student getStudent(){
		return s;
	}
	public String getmajor(){return major;}

	public String toString() {
		String temp="";
		for(int i = 0 ; i<selCourse.length; i++){
			temp=temp+selCourse[i]+"\n";
		}
		return s.toString()+" major:"+getmajor()+"\n"+temp;
	}




    /************End****************/

  
}
