package step1;

public class Task {
	public String inversion(String str){
		/********* Begin *********/
		if(str == null || str.length() < 2) return str;
		return inversion(str.substring(1, str.length()))+ str.charAt(0);	
		/********* End *********/
	}
}
