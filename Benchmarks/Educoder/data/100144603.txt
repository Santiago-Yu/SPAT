package step1;

public class Task {
	public String inversion(String str){
		/********* Begin *********/
		char []s=str.toCharArray();
                char []t=new char[s.length];
                int i,j;
                for(i=0,j=s.length;i<s.length;i++,j--){
                    t[i]=s[j-1];
                }
		String string=String.copyValueOf(t);
                return string;
		
		
		/********* End *********/
	}
}
