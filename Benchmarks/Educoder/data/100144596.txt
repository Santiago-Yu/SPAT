package step1;

public class Task {
	public String inversion(String str){
		/********* Begin *********/
		int len = str.length();
  	     char[] s2 = new char[len]; 
  	     for(int i=0;i<len;i++){
  	    	 s2[len-1-i] =str.charAt(i);
  	     }
  	    String str1 = String.copyValueOf(s2);
  	      return str1 ;

		
		
		/********* End *********/
	}
}
