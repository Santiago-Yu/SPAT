package step3;

public class FindMaxNum {
         int max;// 找出三个整数的最大值
    public int FindMaxNum(int a, int b, int c){
      if (a>b)
       b=a;
      if(b>c)
          return b;
      else
          return c; 
      // 请在此添加实现代码
	}
}

