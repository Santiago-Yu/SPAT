package step3;
import java.util.Scanner;
public class FindMaxNum {
    // 找出三个整数的最大值
    public static void maan(String[] args){
        Scanner sc=new Scanner(System.in);
        int a=sc.nextInt();
        int b=sc.nextInt();
        int c=sc.nextInt();
         FindMaxNum vo=new FindMaxNum();
        System.out.println(vo.FindMaxNum(a, b, c));
    }
    public int FindMaxNum(int a, int b, int c){
        // 请在此添加实现代码
       if(a>b&&a>c){
       return a;}
        else if(b>c&&b>a){
            return b;
        }else{
        return c;}
        
       
	}
}
