package setp10;
import java.util.Arrays;

public class HelloWorld {
    public static void main(String[] args) {
     HelloWorld fangfa1=new HelloWorld();
        int[] scores={79,52,98,81};

        //调用方法，传入成绩数组，并获取成绩的个数
        fangfa1.count(scores);

        
    }

    /*
     * 功能：将考试成绩排序并输出，返回成绩的个数
     * 
     */
    public static void count(    int  [] score1    ){
       // 数组从小到大排序处理
        for (int j=3;j>=1;j--){
      if(score1[j]<score1[j-1])
      {
        int iTemp=score1[j];
        score1[j]=score1[j-1];
        score1[j-1]=iTemp;
      }
    }
       // 打印出数组
int b=0;
        for (int i=0;i<score1.length;i++)
      {
            b++;}
        System .out.println("["+score1[0]+", "+score1[1]+", "+score1[2]+", "+score1[3]+"]");
        
      
       System .out.println("共有"+b+"个成绩信息！");
    
       //返回数组中元素的个数

    }
}