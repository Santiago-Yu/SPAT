#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int k;
    cin >> k;
    int sum;
    if (k%3==0)
        sum=pow(3,k/3);
    else if (k%3==1)
        sum=pow(3,k/3-1)*4;
    else
        sum=pow(3,k/3)*2;
    cout <<  sum;
}