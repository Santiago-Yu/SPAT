#include<stdio.h>
int max(int x,int y)
{
	if(x>y) return x;
	else   return y;
}
int main()
{
	int i,j,k,m,n;
	scanf("%d",&k);
	int w[k];
	int c[k+1];
	int f[202]={0};
	for(i=0;i<=k;i++) f[i]=1;
	for(i=2;i<=k;i++)
	{
		for(j=i;j<=k;j++)
		{
			f[j]=max(f[j],f[j-i]*i);
		}
	}
	printf("%d",f[k]);
}