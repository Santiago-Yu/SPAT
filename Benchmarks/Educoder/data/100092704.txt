package step3;

public class FindMaxNum {
    // 找出三个整数的最大值
    public int FindMaxNum(int a, int b, int c){
        if(a>b)
        {
            if (a>c)        
                return a;
        return c;
        }
        if(b>c)
        return b;
        return c;
        
            
        // 请在此添加实现代码
	}
}
