#include<stdio.h>
#include<math.h>
int main()
{
    int n ;
    while(scanf("%d",&n)!=EOF)
    {
    int a = (3-n%3)%3;
    int b = (n-a*2)/3;
    int c = pow(2,a)*pow(3,b);
    printf("%d",c);
    }
}
