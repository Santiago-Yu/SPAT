package step3;

import java.io.File;
import java.io.FileReader;
import java.io.FileWriter;
import java.io.IOException;

public class DeleteFiles {
    public static void start() throws IOException {
        StringBuilder data = new StringBuilder("");
        /********** Begin *********/
   File f1 = new File("src/step3/readme.txt");
FileReader fileReader = new FileReader(f1);
int read = fileReader.read();
        //写入readmeCopy.txt文件
File f2 = new File("src/step3/readmeCopy.txt");
        f2.createNewFile();
FileWriter fileWriter = new FileWriter(f2, false);
    while (read != -1) {   //循环读取，直到read的值为-1，表示文件读到末尾
    fileWriter.write(read);
        read = fileReader.read();   //读取下一个字符
    }
        fileReader.close();
fileWriter.close();
        //删除readme.txt文件
File f4 = new File("src/step3/readme.txt");
if (f4.exists()) {   //如果文件存在
   f4.delete();
}

        /********** End *********/
    }
}
