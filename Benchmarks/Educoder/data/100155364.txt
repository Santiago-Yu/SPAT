package task3;

public class Box {

    double width;
    double height;
    double depth;

    /**
     * 代表长方体
     */
    public static final int RECTANGLE = 1;

    /**
     * 圆柱体
     */
    public static final int CYLINDER = 2;

    public double volume(int boxType) {
        double vol;
        switch (boxType){
            	case 1 : return (width * height * depth);
            	case 2 : return (width * width * height * 3.14);
        }
      return 0;//TODO write your code here
    }

}
