#include <stdio.h>
#include <string.h>
#include <math.h>
main()
{
	int a,b,c,d,e,f,g,i,j;
	scanf("%d",&a);
	c=a%3;
	d=(a-c)/3;
	if(c==1)
	{
		e=pow(3,d-1)*2*2;
	}
	if(c==2)
	{
		e=pow(3,d)*2;
	}
	if(c==3)
	{
		e=pow(3,d);
	}
	printf("%d",e);
}