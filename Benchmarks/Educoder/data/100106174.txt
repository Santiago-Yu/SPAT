package Step1;

public class UnicodeDemo {
     public String Encoding(String s) {
       	
		char x[] = s.toCharArray();
		char c=92;
		for (int j = 0; j < x.length; j++) {
			 System.out.print(c+"u" + Integer.toHexString(x[j]));
		}
         System.out.println(); 
         return s;
     }
}
