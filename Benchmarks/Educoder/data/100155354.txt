package task3;

public class Box {

    double width;
    double height;
    double depth;

    /**
     * 代表长方体
     */
    public static final int RECTANGLE = 1;

    /**
     * 圆柱体
     */
    public static final int CYLINDER = 2;

    double volume(int boxType) {
        //TODO write your code here
        double V;
        switch(boxType){
            case 1:
                {V=width*height*depth;
                break;}
            case 2:
                {V=3.14*width*width*height;
                break;}
            default:
                return 0;
        }
        return V;
            
    }

}
