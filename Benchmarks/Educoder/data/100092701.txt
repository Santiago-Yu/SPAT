package step3;

public class FindMaxNum {
    // 找出三个整数的最大值
    public int FindMaxNum(int a, int b, int c){
        // 请在此添加实现代码
        FindMaxNum sort = new FindMaxNum();
        int max;
        if(a>b&&a>c)
            return max=a;
        if(b>c&&b>a)
            return max=b;
        else
            return max=c;
            
	}
}
