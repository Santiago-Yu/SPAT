package setp15;

/**
 * @author tangzhiqiang
 * @date 2018-04-25 22:31
 */
public class HelloWorld {
    public static void main(String[] args) {

HelloWorld f=new HelloWorld();
        f.fangfa();
        // 调用无参的方法
        
        // 调用带有一个字符串参数的方法
String g="educoder";
        f.fangfa(g);
        // 调用带有一个整型参数的方法
        int p=666;
        f.fangfa(p);
    }
    public static void fangfa(){
    System.out.println("无参的print方法");
    }
    // 无参print方法的定义

    public static void fangfa(String l){
    System.out.println("带有一个字符串参数的print方法，参数值为："+l);
    }
    // 字符串print方法的定义
    public static void fangfa(int p)
        {
    System.out.println("带有一个整型参数的print方法，参数值为："+p);
    }
    
    // 整型print方法的定义
    
}
