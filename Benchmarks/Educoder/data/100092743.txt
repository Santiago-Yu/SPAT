package step3;

public class FindMaxNum {
    // 找出三个整数的最大值
    public int FindMaxNum(int a, int b, int c){
        // 请在此添加实现代码
        int q;
        if (a>b){
            q=a;
        }else{
            q=b;
        }int max;
        if (q>c){
        max=q;
    }else{
        max=c;
    }
    return max;

        
	}
}
