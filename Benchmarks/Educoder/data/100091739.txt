package step3;

public class PalindromeJudge {
	// 判断一个五位正整数是否是回文数
	public boolean PalindromeJudge(int num){
		// 请在此添加实现代码
        int a=num%10;
        int a1=num/10000;
        int b=num/10%10;
        int b1=num/1000%10;
        if(a==a1&&b==b1) return true;
        else return false;
}
}