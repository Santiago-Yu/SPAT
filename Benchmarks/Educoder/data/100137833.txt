package case5;

import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.util.Date;
import java.util.Scanner;
import java.util.Calendar;

public class DateTest {
	public static void main(String[] args) throws ParseException {
		// 键盘录入你的出生年月日 格式为yyyy-MM-dd
		// 把该字符串转换为一个日期
		// 通过该日期得到一个毫秒值
		// 获取2020年10月1日的毫秒值
		// 两者想减得到一个毫秒值
		// 把该毫秒值转换为天 打印输出
		Scanner sc = new Scanner(System.in);
		String line = sc.nextLine();
		/********* Begin *********/
         SimpleDateFormat ft = new SimpleDateFormat("yyyy-MM-dd");
         Date z=ft.parse(line);
        long zz=z.getTime();
        zz=zz/86400000;               
        Calendar a=Calendar.getInstance();
        a.set(2020,9,1);
        Date as=a.getTime();
        long sd=as.getTime();
        sd=sd/86400000;
        SimpleDateFormat sdf=new SimpleDateFormat("你的出生日期距离yyyy年M月d日:");
        System.out.println(sdf.format(as)+(sd-zz)+"天");

        
		/*StringBuffer bri = new  StringBuffer(line);
        bri.deleteCharAt(4);
        bri.deleteCharAt(6);
        Integer yyy = new Integer(bri.substring(0,4));
        Integer mmm = new Integer(bri.substring(5,6));
        Integer ddd = new Integer(bri.substring(7,8));
        int yy = yyy;
        int mm = mmm;
        int dd = ddd;
        //System.out.println(dd);
        Date calendar = new Date(yy,mm,dd);
        //calendar.setTime();
        long d = calendar.getTime();
        Date data = new Date(2020,10,2);
        //data.setTime();
        long s = data.getTime();
        if(s == d)
            System.out.println("你的出生日期距离2020年10月1日:"+0+"天");
        else
        d = (s-d)/86400000;
        System.out.println("你的出生日期距离2020年10月1日:"+d+"天");*/
		/********* End *********/
	}
}