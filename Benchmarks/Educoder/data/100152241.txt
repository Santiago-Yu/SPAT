package step1;
import org.junit.Test;
import static org.junit.Assert.assertEquals;
import static org.junit.Assert.*;

public class ElevatorTest {
    @Test
    public void test(){
        Elevator a = new Elevator(10,1);
        assertTrue(a.moveUP());
    }
}

