package step4;
import  step4.WuMingFen;
import java.util.Scanner;

public class Test {
	
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		String theMa = sc.next();
		int quantity = sc.nextInt();
		boolean likeSoup = sc.nextBoolean();

		WuMingFen f1 = new WuMingFen();//使用三个参数的构造方法创建WuMingFen对象  取名 f1
		WuMingFen f2 = new WuMingFen();//使用两个参数的构造方法创建WuMingFen对象  取名 f2
		WuMingFen f3 = new WuMingFen();//使用无参构造方法创建WuMingFen对象  取名 f3

        f1.WuMingFen1(theMa,quantity,likeSoup);//分别调用三个类的 check方法
		f2.WuMingFen2(theMa,quantity);
        f3.WuMingFen3();
	
    }
}
