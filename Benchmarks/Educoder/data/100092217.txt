package step2;

public class IntCalculate {
	// 根据题目要求返回输出结果
	public int IntCalculate(int a, int b){
		// 请在此添加实现代码
        int c;
        int d;
        c = a + b;
        d = a%b;
        if(c > 1000||d == 0)
            return a;
        else
            return b;
	}
}
