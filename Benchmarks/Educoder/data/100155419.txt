package task4;

public class Box {

    double width;
    double height;
    double depth;
    double vol;

    /*int boxType;
	public Box (int boxType){
       // double vol;
    	if(boxType == 1){
        	vol = 1;
        }
        else if(boxType == 2){
        	vol = 3.14;
        }
    } 
    /**
     * 代表长方体
     */
    public static final int RECTANGLE = 1;

    /**
     * 圆柱体
     */
    public static final int CYLINDER = 2;
int boxType;
	public Box (int boxType){
       // double vol;
    	if(boxType == 1){
        	vol = 1;
        }
        else if(boxType == 2){
        	vol = 3.14;
        }
    }
    public double volume() {
        //double vol;
        vol = vol * width * height * depth;
        return vol;//TODO your code
    }

}
