package step1;

public class Task {
	
	public void sort(Integer[] arr){
		/********* Begin *********/
        if(isSorted(arr)){
            for(int y = 0 ; y < arr.length ;y++){
                System.out.print(arr[y]+" ");
            }
            System.out.println() ;
            return ;
        }else{
            while(!isSorted(arr)){
                Sorting(arr) ;
            }
        }
		/********* End *********/
	}
    public void Sorting(Integer[] arr){
    	for(int i = 0 ; i < arr.length-1; i ++){
            if(arr[i] > arr[i+1]){
                Integer temp = 0 ;
                temp = arr[i] ;
                arr[i] = arr[i+1] ;
                arr[i+1] = temp ;
                for(int x = 0 ; x < arr.length ;x++){
                    System.out.print(arr[x]+" ");
                }
                System.out.println() ;
            }

         }
        for(int j = arr.length -1 ; j >0 ; j--){
            if(arr[j] < arr[j-1]){
                Integer temp = 0 ;
                temp = arr[j] ;
                arr[j] = arr[j-1] ;
                arr[j-1] = temp ;
                for(int y = 0 ; y < arr.length ;y++){
                    System.out.print(arr[y]+" ");
                }
                System.out.println() ;
            }

        }
    }
    public  boolean isSorted(Integer[] data) {
    boolean flag = false;
    //数组是否为升序
        for (int i = 0; i < data.length-1; i++) {
            if (data[i] == Math.min(data[i], data[i + 1])) {
                flag = true;
            } else {
                flag = false;
                break;
            }
        }
        return flag ;
    }
}