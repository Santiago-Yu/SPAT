package task3;
import java.util.Scanner;

public class Box {

    double width;
    double height;
    double depth;
    static int boxType = 1;

    /**
     * 代表长方体
     */
    public static final int RECTANGLE = 1;

    /**
     * 圆柱体
     */
    public static final int CYLINDER = 2;

    public static void main(String[] args) {
    Scanner sc = new Scanner(System.in);
    Box box = new Box();
    box.width = sc.nextDouble();
    box.height = sc.nextDouble();
    box.depth = sc.nextDouble();
    box.boxType = sc.nextInt();
    double x = box.volume(boxType);
    System.out.println("the volumn is:"+x);
    }
    
    public double volume(int boxType) {
        
        switch(boxType){
            case 1:
                return width*height*depth;
            case 2:
                return width*height*depth*3.14;
        }
        return 0;
        //TODO write your code here
    }

}
