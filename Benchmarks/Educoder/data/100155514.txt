package task4;

public class Box {

    double width;
    double height;
    double depth;
	double volume;
    int boxType;
    public Box(int boxType){
        this.boxType=boxType;
    }
     
 
        

    /**
     * 代表长方体
     */
    public static final int RECTANGLE = 1;

    /**
     * 圆柱体
     */
    public static final int CYLINDER = 2;

    public double volume() {
        //TODO your code
        switch(boxType){
        case 1:volume=width*height*depth;
                return volume;
            case 2:volume=height*3.14*width*depth;
                return volume;  
    }
		return 0;
}
}
