#include <bits/stdc++.h>
#include <stdio.h>
#include <string.h>
using namespace std;
int i;
char s1[500];
char s2[500];
int len1;
int len2;

int sum;

int main()
{
    char ss;
    int ii = 0,jj = 0;
	int max=0;
    while(scanf("%c",&ss)!=EOF)
    {
        if(ss == '\n') break;
        s1[ii] = ss;
        ii++;
    }
    s1[ii] = '\0';
    while(scanf("%c",&ss)!=EOF)
    {
        if(ss == '\n') break;
        s2[jj] = ss;
        jj++;
    }
    s2[jj] = '\0';
    len1 = strlen(s1);
    len2 = strlen(s2);
    i = 0;
    int count;
    int temp;
    int temp2;
    if(len1==0||len2==0)return 0;
    for(i=0;i<len1;i++){
    	for(int j=0;j<len2;j++)
    	{
    		count = 0;
    		temp = i;
            temp2 = j;
    		while(s2[temp2]==s1[temp]&&temp2<len2&&temp<len1){
    			count ++;
    			temp++;
                temp2++;
    		}
    		if(max<count)max = count;
    	}
    }
    printf("%d",max);

}