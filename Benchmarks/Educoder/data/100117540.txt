package setp7;
import java.util.Scanner;
public class HelloWorld {
 public static void main(String[] args) {
      // 调用calcAvg()方法，并将返回值保存在变量avg中
      double avg=calcAvg();
      System.out.println("平均成绩为：" + avg);
  }

  // 定义一个返回值为double类型的方法
  public static double  calcAvg() {
      double avg1 =87.75 ; // 计算平均值
return avg1;
      // 使用return返回值
   		/********** End **********/
    }
}