package step3;

public class Task {

	 public int countOfShips(char[][] ferry) {
	  /**************BEGIN************/
          int m = 0 ;
         if(ferry.length != 0)
         {
	 for(int i = 0 ; i < ferry.length ; i++ ){
      for(int j = 0 ; j < ferry[i].length ; j++ ){
          if(ferry[i][j] == '+' ) {
              int x = 0 ;
              while( j+1 <ferry[i].length  &&ferry[i][j+1] == '+'  ){
                 if(x == 0 ) {m++ ; x++ ;}
                  j += 1 ;
              }
          }
      }
     }
      for(int j = 0 ;  j < ferry[0].length ; j++ ){
        for(int i = 0 ; i < ferry.length ; i++ ){
          if(ferry[i][j] == '+' ) {
              int x = 0 ;
              while( i+1 <ferry.length && ferry[i+1][j] == '+'  ){
                 if(x == 0 ) {m++ ; x++ ;}
                  i += 1 ;
              }
          }
      }
     }
	 for(int i = 0 ; i < ferry.length ; i++ ){
      for(int j = 0 ; j < ferry[i].length ; j++ ){
          if(ferry[i][j] == '+' && 
             ((i+1 < ferry.length && ferry[i+1][j] != '+' )|| i == ferry.length-1 )&& 
             ((i-1 >= 0 && ferry[i-1][j] != '+')|| i == 0 ) && 
              ((j+1 < ferry[i].length &&ferry[i][j+1] != '+' ) || j == ferry[i].length -1)&& 
              ((j-1 >= 0 &&ferry[i][j-1] != '+') || j == 0 ) ) {
              m++ ;
              }
          }
      }
         }
      return m ;
        
      /***********END ***************/
	 }
}
