package step3;

import java.io.File;
import java.io.FileReader;
import java.io.FileWriter;
import java.io.IOException;

public class DeleteFiles {
    public static void start() throws IOException {
        StringBuilder data = new StringBuilder("");
        /********** Begin *********/
        //读取readme.txt文件
         FileReader inputStream = null;
    FileWriter outputStream = null;

 File file = new File("src/step3/readmeCopy.txt");   ////file对象指向src文件夹下的step1文件夹下的Hello World.txt文件（此时此文件不存在）

        
  try {
      inputStream = new FileReader("src/step3/readme.txt");
      outputStream = new FileWriter(file);

      int c;
      //1.每次读取一个字符
      while ((c = inputStream.read()) != -1) {
        //2。每次写入一个字符  
        outputStream.write(c);
      }
   
    } finally {
      if (inputStream != null) {
        inputStream.close();
      }
      if (outputStream != null) {
        outputStream.close();
      }
    }




        //写入readmeCopy.txt文件


        //删除readme.txt文件
File file1 = new File("src/step3/readme.txt");   //file对象指向src文件夹下的step1文件夹下的Hello World.txt文件

file1.delete(); //删除该文件，并输出布尔值


        /********** End *********/
    }
}
