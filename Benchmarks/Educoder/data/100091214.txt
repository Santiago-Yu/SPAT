package step1;

public class AbsoluteOfTwo {

    // 求两个整数差的绝对值
    public int AbsoluteOfTwo(int x, int y) {
        if(x>y)
            return (x-y);
        else
            return (y-x);// 请在此添加实现代码
    }
}
