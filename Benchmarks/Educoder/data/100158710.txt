#include <iostream>
#include<stdio.h>
#include<string.h>
using namespace std;
int main()
{
    int k,h=1;
    cin>>k;
    if(k==1) cout<<"0";
    else if(k==2) cout<<"1";
    else if(k==3) cout<<"2";
    else if(k==4) cout<<"4";
    else
    {
        for(;;)
        {k=k-3;
         h=h*3;
        if(k<5)break;}
        h=h*k;
    cout << h;}
    return 0;
}
