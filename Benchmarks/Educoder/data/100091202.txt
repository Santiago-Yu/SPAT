package step1;

public class AbsoluteOfTwo {

    // 求两个整数差的绝对值
    public int AbsoluteOfTwo(int x, int y) {
        int num;
    	num=x-y;
    	if(num>=0) {
    		return num;
    	}else {
    		return -num;
    	}
        // 请在此添加实现代码
    }
}
