package step2;

public class SegmentFunCal {
	// 求分段函数的值
	public int SegmentFunCal(int x){
		int y;
        if (x>0){
        return y=x+2;
        }else if (x==0){
            return y=x-2;
        } else
           return y=x-4;
            // 请在此添加实现代码
            
	}
}
