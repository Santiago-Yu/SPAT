package step3;

import java.io.File;

public class Task {
	
	/********** BEGIN **********/
	public void showDirStructure(File file)	{
			int count = file.getPath().split("/").length;
        for(int k=0;k<(count-3)*2;k++)
            System.out.print(" ");
		if(file.isDirectory()) {
            
			System.out.println("+--"+file.getName());
			File [] fs = file.listFiles();
			for(File f:fs) {
				if(f.isDirectory()) {

					showDirStructure(f);
                      
				}else {
					String fname = f.getName();
					int index = fname.indexOf(".");
					String p = fname.substring(index+1,fname.length());
					if(p.equals("jpg")||p.equals("bmp")||p.equals("png")) {
						int count1 = f.getPath().split("/").length;
		                for(int k=0;k<(count1-3)*2;k++)
		                 System.out.print(" ");
						System.out.println("--"+f.getName());
					}
					
					
                  
				}
			}
		}else {
			String fname = file.getName();
			int index = fname.indexOf(".");
			String p = fname.substring(index+1,fname.length());
			if(p.equals("jpg")||p.equals("bmp")||p.equals("png")) {
				int count2 = file.getPath().split("/").length;
                for(int k=0;k<(count2-3)*2;k++)
                    System.out.print(" ");
                System.out.println("--"+file.getName());
			}
				
			
             
		}






	}
	

	/********** END **********/
}
