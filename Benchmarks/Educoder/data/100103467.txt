package step3;

import java.io.File;
import java.io.FileReader;
import java.io.FileWriter;
import java.io.IOException;

public class DeleteFiles {
    public static void start() throws IOException {
        StringBuilder data = new StringBuilder("");
        /********** Begin *********/
        //读取readme.txt文件
        
	File file = new File("src/step3/readme.txt");
	FileReader fileReader = new FileReader(file);
        //写入readmeCopy.txt文件
        File fileW = new File("src/step3/readmeCopy.txt");
        
	FileWriter fileWriter = new FileWriter(fileW,false);
	String string1 = "Hello World";
	String string2 = "Java is great";
        fileWriter.write(string1 +"\n" + string2 + "\n");
	fileWriter.close();
        //删除readme.txt文件
	
        if (file.exists()) {   //如果文件存在    
     file.delete();   //删除该文件，并输出布尔值
}
	
        /********** End *********/
    }
}
