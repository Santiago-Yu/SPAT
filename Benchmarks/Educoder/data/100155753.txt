package task6;

public class ScaleBox extends Box        {  //继承Box类
    private String unit;
    //TODO 定义私有变量unit

    public ScaleBox(int boxType) {
        super(boxType);
    }

    //实现 getUnit、setUnit方法
	public int setUnit(String unit) {
        this.unit=unit;
        return 0;
    }
 	public String setUnit() {
        return unit;
    }

	//实现printVolumn()方法，在该方法中调用父类的方法计算体积 最后输出体积
	public void printVolumn() {
       // String 
       System.out.println("the volumn is:"+super.volume()+setUnit());
    }

}
