package step2;

import java.util.Scanner;

public class NameSearch {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        /********** Begin *********/
            String data= scanner.nextLine();
          String lisi=scanner.nextLine();
        int firstposition=data.indexOf(lisi);
        int oldposition=-1;
       
        
        while(firstposition>-1){
           
            oldposition+=firstposition+1;
            
             System.out.println(oldposition);
            if(firstposition+2>data.length())
                break;
             data=data.substring(firstposition+1);
            firstposition=data.indexOf(lisi);
           
         
             
        }


        /********** End *********/
    }
}