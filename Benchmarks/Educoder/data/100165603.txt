package step3;
import step1.Date;
public class Course {
    private int code; //课程编号
    private String courseName; //课程名称
    private Date testTime;//考试日期
    private int score;//考试成绩
   /******Begin**************/
	public Course(){
		testTime = new Date();
	}
	public Course(int code, String courseName, Date testTime, int score){
		this.testTime = testTime;
		this.code = code;
		this.courseName = courseName;
		this.score = score;
	}
	public Course(int code, String courseName, Date testTime, int year, int y, int m, int d){
		this.testTime = new Date(y, m, d);
		this.code = code;
		this.courseName = courseName;
		this.score = score;
	}
	public Course(int code, String courseName, int y, int m, int d, int score){
		this.testTime = new Date(y, m, d);
		this.code = code;
		this.courseName = courseName;
		this.score = score;
	}
	public String toString(){
		if(courseName == null)
			return code+"  "+ testTime.toString() + " "+ score;
		return code+" "+ courseName + " "+ testTime.toString() + " "+ score;
	}

   /******End************/

}

