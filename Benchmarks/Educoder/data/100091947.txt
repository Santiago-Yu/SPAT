package step1;
 
public class IsOfVowels {
	// 判断输入字符是否是元音字母
     
	public boolean IsOfVowels(char ch){
		// 请在此添加实现代码
          String ch1="aeiouAEIOU";
        for(int i=0;i<ch1.length();i++){
       
        if(ch==ch1.charAt(i)) 
            return true;
        }
           return false;
	}
}