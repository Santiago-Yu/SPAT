package step2;

public class Equation {
		double a, b, c; //一元二次方程的三个系数
		public Equation(){}
		public Equation(double a,double b,double c){
            this.a=a;
            this.b=b;
            this.c=c;
			//YOUR CODE HERE
		}
		/*************Begin*******************/
	    //YOUR CODE HERE
		public double getX1()
        {
            double derta,x1;
            derta= b*b-(4.0*a*c);
            x1=(-b+Math.sqrt(derta))/(2*a);
            return x1;
        }
    	public double getX2()
        {
            double derta2,x2;
            derta2= b*b-(4.0*a*c);
            x2=(-b-Math.sqrt(derta2))/(2*a);
            return x2;
        }
    	public double deta()
        {
            double derta3;
            derta3= b*b-(4.0*a*c);
            return derta3;
        }

    	  /***********End*******************/
		
		
	} 

