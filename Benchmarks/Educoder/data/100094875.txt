package step2;

public class AdditionCalculator {
	public void addition(int a, int n) {
		/********** Begin *********/
int sum=0;
        while(n>=0){
        sum=sum+a*(int)Math.pow(10,n-1)+a*(int)Math.pow(10,n-2)+a*(int)Math.pow(10,n-3)+a*(int)Math.pow(10,n-4);
        n--;
        }
System.out.print(sum);

		/********** End *********/
	}
}
