package step1;

public class Task {
	public String inversion(String str){
		/********* Begin *********/
		int len=str.length();
		for(int i=len-1;i>=0;i--)
			str+=str.substring(i,i+1);
		str=str.substring(len);
		return str;	
		/********* End *********/
	}
}
