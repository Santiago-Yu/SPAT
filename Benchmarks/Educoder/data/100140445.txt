package step3;
import java.util.Scanner;
public class HelloWorld {
	public static void main(String[] args){
    Scanner scanner=new Scanner(System.in);
        String fileName=scanner.nextLine();
        String email=scanner.nextLine();
         judge(fileName,email);
    }
	public static void judge(String fileName,String email){
        if(fileName.length()>=5&&email.length()>=4){
        String d=".com";
        int e=email.length()-4;
        String c=".java"; 
    int a=fileName.length()-5;
        	String b=fileName.substring(a,fileName.length());
    	if(b.equals(c)&&fileName.length()>5){
        System.out.println("Java文件名正确");
        }
         else{
         System.out.println("Java文件名无效");
         }
        String f=email.substring(e,email.length());
        if(f.equals(d)){
            System.out.println("邮箱名正确");
        }
        else{
          System.out.println("邮箱名无效");
        }
        }
        else{
            System.out.println("Java文件名无效");
              System.out.println("邮箱名无效");
        }
    }
	
}
	
	
