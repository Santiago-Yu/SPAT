package step1;

public class Yard {
		int len=0,wid=0;
       public Yard(int length,int width){
    	   len=length;
    	   wid=width;
       }
       public float getPerimeter(){
    	   return (len+wid)*2;
       }
       public float getAcreage(){
    	   return wid*len;
       }
	}