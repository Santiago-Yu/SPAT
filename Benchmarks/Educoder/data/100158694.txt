#include <stdio.h>
#include <math.h>
#include <string.h>
main()
{
	int x,a,b;
	scanf("%d",&x);
	a=x%3;
	x=(x-a)/3;
	if(a==0)
	{
		b=pow(3,x);
		printf("%d",b);
	}
	if(a==1)
	{
		b=2*2*pow(3,x-1);
		printf("%d",b);
	}
	if(a==2)
	{
		b=2*pow(3,x);
		printf("%d",b);
	}
	
}