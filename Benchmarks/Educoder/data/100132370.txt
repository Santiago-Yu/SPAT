package step1;

public class Task {
	public  int[] dailyTemps(Integer[] temperatures){
		/********** BEGIN **********/
        int[] reArr = new int[temperatures.length];
		for (int i = 0;i < temperatures.length;i++){
            int nextDay = 0;
            for (int j = i + 1;j<temperatures.length;j++){
                if(temperatures[j] > temperatures[i]){
                    nextDay = j - i;
                    break;
                }
            }
            reArr[i] = nextDay;
        }
		
		

		return reArr;
		/********** END **********/
	}
}
