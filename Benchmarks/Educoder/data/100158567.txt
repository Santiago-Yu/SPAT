#include <iostream>
#include<cstdio>
#include<string>
using namespace std;
int main()
{   int max1=0;
    string s1,s2,s3;
    cin>>s1;
    cin>>s2;
    if(s1.length()<s2.length())
    {   s3=s1;
        s1=s2;
        s2=s3;}
    for(int i=0;i<s1.length();i++)
        for(int j=0;j<s2.length();j++)
        {
            if(s2[j]==s1[i])
            {   int flag=1;
                int j1=j;
                int i1=i;
                while(1)
                {
                    if(s1[i1++]==s2[j++])
                    {
                        flag++;
                    }
                    else
                        break;
                }
             if(flag>max1)
                 max1=flag;
            }
        }
          printf("%d",max1-1);
 return 0;
    
}