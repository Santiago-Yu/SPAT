package step2;

public class TriangleJudge{

    // 判断三条边能否构成一个三角形
    public boolean TriangleJudge(int a, int b, int c) {
        // 请在此添加实现代码
        int d;
        boolean e;
        if(a+b>c&&a+c>b&&c+b>a)
            d=1;
        else
            d=0;
        if(d==1)
            e=true;
        else
            e=false;
        return e;
        
        
    }
}
