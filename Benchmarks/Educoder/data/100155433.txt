package task4;

public class Box {

    double width;
    double height;
    double depth;
    int boxType;
 
    public static final int RECTANGLE = 1;   //长方体

    public static final int CYLINDER = 2;    //圆柱体
    public Box(int boxType)   { this.boxType = boxType;} 

    public double volume() {
      //TODO your code
       double volume=0;
        if(boxType==1)
        {volume=width*height*depth;}
         
        if(boxType==2)  
        {volume=3.14*width*depth*height;} 
       return volume;
     }   
}     
      
        
      


